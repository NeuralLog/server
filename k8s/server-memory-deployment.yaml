apiVersion: apps/v1
kind: Deployment
metadata:
  name: neurallog-server-memory
  labels:
    app: neurallog
    component: server
    storage: memory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: neurallog
      component: server
      storage: memory
  template:
    metadata:
      labels:
        app: neurallog
        component: server
        storage: memory
    spec:
      containers:
      - name: server
        image: neurallog/server:latest  # Replace with your image repository
        ports:
        - containerPort: 3030
          name: http
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: neurallog-config
              key: NODE_ENV
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: neurallog-config
              key: PORT
        - name: DEFAULT_NAMESPACE
          valueFrom:
            configMapKeyRef:
              name: neurallog-config
              key: DEFAULT_NAMESPACE
        - name: STORAGE_TYPE
          value: "memory"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: neurallog-server-memory
  labels:
    app: neurallog
    component: server
    storage: memory
spec:
  ports:
  - port: 80
    targetPort: 3030
    name: http
  selector:
    app: neurallog
    component: server
    storage: memory
